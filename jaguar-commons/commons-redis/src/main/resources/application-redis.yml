spring:
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    lettuce:
      pool:
        min-idle: 5
        max-idle: 10
        max-active: 10
    replica:
      enable: false
      slaves: