server:
  servlet:
    session:
      timeout: 3600
      cookie:
        name: JAGUAR-SESSION-ID
    application-display-name: ${spring.application.name}
  tomcat:
    uri-encoding: UTF-8

spring:
  http:
    encoding:
      charset: UTF-8
      force: true
