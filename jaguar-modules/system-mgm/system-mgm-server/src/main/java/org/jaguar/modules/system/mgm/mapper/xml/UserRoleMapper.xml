<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jaguar.modules.system.mgm.mapper.UserRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.jaguar.modules.system.mgm.model.UserRole">
        <id column="id_" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <association property="role" javaType="org.jaguar.modules.system.mgm.model.Role">
            <result column="role_name" property="roleName"/>
            <result column="role_data_scope" property="dataScope"/>
            <result column="role_locked" property="roleLocked"/>
        </association>
        <association property="user" javaType="org.jaguar.modules.system.mgm.model.User">
            <result column="user_account" property="userAccount"/>
            <result column="user_phone" property="userPhone"/>
            <result column="user_email" property="userEmail"/>
            <result column="user_nick_name" property="userNickName"/>
        </association>
    </resultMap>

    <select id="listWithRoleByUserId" resultMap="BaseResultMap">
        SELECT ur.*,
               r.role_name,
               r.role_data_scope,
               r.role_locked
        FROM jaguar_modules_system_user_role ur
                 LEFT JOIN jaguar_modules_system_role r ON r.id_ = ur.role_id
        WHERE ur.deleted_ = 0
          AND r.deleted_ = 0
          AND r.role_locked = 0
          AND ur.user_id = #{userId}
    </select>

    <select id="listWithUserByRoleId" resultMap="BaseResultMap">
        SELECT ur.*,
               u.user_account,
               u.user_phone,
               u.user_email,
               u.user_nick_name
        FROM jaguar_modules_system_user_role ur
                 LEFT JOIN jaguar_modules_system_user u ON u.id_ = ur.user_id
        WHERE ur.deleted_ = 0
          AND u.deleted_ = 0
          AND ur.role_id = #{roleId}
    </select>

</mapper>
