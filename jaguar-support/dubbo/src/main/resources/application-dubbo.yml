dubbo:
  application:
    name: ${project.artifactId}
  registry:
    address: zookeeper://127.0.0.1:2181
    file: ./dubbo/${dubbo.application.name}.cache
  protocol:
    name: dubbo
    port: 18080
    threads: 200
    dispatcher: message
  consumer:
    timeout: 30000