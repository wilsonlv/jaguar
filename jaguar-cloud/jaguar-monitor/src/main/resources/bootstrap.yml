server:
  port: 18888

spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      discovery:
        server-addr: jaguar-register:8848
      config:
        file-extension: yml
  profiles:
    active: @profiles.active@